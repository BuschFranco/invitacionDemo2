<footer class="footer">
    <div class="container">
        <div class="footer-content animate-fade-in">
            <div class="footer-celebration">
                <span class="crown-icon">👑</span>
                <span class="footer-name">Isabella</span>
                <span class="crown-icon">👑</span>
            </div>
            <div class="footer-date">15 · 03 · 2026</div>
            <div class="footer-age">Mis Quince Años</div>
            <div class="footer-message">¡Gracias por ser parte de mi sueño hecho realidad!</div>
            <div class="footer-hearts">💖 ✨ 💖</div>
        </div>
    </div>
</footer>

<style>
    .footer {
        background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
        padding: var(--spacing-xl) 0;
        text-align: center;
        color: white;
        position: relative;
        overflow: hidden;
    }

    .footer::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="20" cy="20" r="2" fill="rgba(255,255,255,0.1)"/><circle cx="80" cy="40" r="1.5" fill="rgba(255,255,255,0.1)"/><circle cx="40" cy="70" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="70" cy="80" r="2.5" fill="rgba(255,255,255,0.1)"/></svg>');
        animation: sparkleMove 20s linear infinite;
    }

    .footer-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--spacing-md);
        position: relative;
        z-index: 2;
    }

    .footer-celebration {
        display: flex;
        align-items: center;
        gap: var(--spacing-md);
        margin-bottom: var(--spacing-sm);
    }

    .crown-icon {
        font-size: 1.5rem;
        animation: crownGlow 2s ease-in-out infinite alternate;
    }

    .footer-name {
        font-size: var(--font-size-2xl);
        font-weight: 700;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
        letter-spacing: 1px;
    }

    .footer-date {
        font-size: var(--font-size-lg);
        font-weight: 600;
        letter-spacing: 3px;
        color: rgba(255, 255, 255, 0.9);
    }

    .footer-age {
        font-size: var(--font-size-xl);
        font-weight: 600;
        color: #FFD700;
        text-shadow: 0 0 15px rgba(255, 215, 0, 0.5);
        margin: var(--spacing-sm) 0;
    }

    .footer-message {
        font-style: italic;
        font-size: var(--font-size-md);
        max-width: 400px;
        line-height: 1.6;
        color: rgba(255, 255, 255, 0.9);
        margin: var(--spacing-md) 0;
    }

    .footer-hearts {
        font-size: 1.5rem;
        margin-top: var(--spacing-sm);
        animation: heartPulse 1.5s ease-in-out infinite;
    }

    @keyframes crownGlow {
        0% { 
            filter: drop-shadow(0 0 5px rgba(255, 215, 0, 0.5));
            transform: scale(1);
        }
        100% { 
            filter: drop-shadow(0 0 15px rgba(255, 215, 0, 0.8));
            transform: scale(1.1);
        }
    }

    @keyframes heartPulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }

    @keyframes sparkleMove {
        0% { transform: translateX(-100%); }
        100% { transform: translateX(100%); }
    }

    @media (max-width: 768px) {
        .footer-celebration {
            flex-direction: column;
            gap: var(--spacing-sm);
        }

        .footer-name {
            font-size: var(--font-size-xl);
        }

        .footer-message {
            font-size: var(--font-size-sm);
            padding: 0 var(--spacing-md);
        }
    }
</style>